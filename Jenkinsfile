pipeline {
      agent {
          sudo docker { image 'nginx:latest' }
      }
      stages {
         stage ('Test') {
               steps {
                  echo "Success!"
               }
         }
      }
} 
    
